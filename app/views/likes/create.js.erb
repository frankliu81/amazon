// select question-like div surrounding the partial
// we are within the same folder, so we can just render like without the folder
$("#<%= dom_id(review)%>_like").html("<%= j render 'like', review: review %>")
